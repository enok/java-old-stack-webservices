# Read by ConnectionFactory from the classpath.
#
# XXX: credentials in a properties file inside the WAR. The real values are
# substituted at deploy time by ansible/deploy.yml. The values below are
# obviously fake local-development placeholders and are safe to commit.
sis.db.url=jdbc:mysql://sis-mysql:3306/campusconnect?useSSL=false&characterEncoding=UTF-8
sis.db.user=sisapp
sis.db.password=CHANGEME-local-dev-only
